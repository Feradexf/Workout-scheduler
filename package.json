{
  "name": "workout-fullstack",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "server": "npm --prefix server run start",
    "client": "npm --prefix client run dev",
    "dev": "npm run server & npm run client"
  }
}

